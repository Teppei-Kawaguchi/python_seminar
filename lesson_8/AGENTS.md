# AGENTS.md - Lesson 8

## 適用範囲

このファイルは `lesson_8/` に適用し、共通規則は `../AGENTS.md` を継承する。

## 固有ルール

- このlessonは準備中である。課題仕様、モデル、Notebook、スライド、評価基準を推測して追加しない。
- `train_titanic.csv` と `test_titanic.csv` は既存の未追跡ユーザー成果物である。内容、名前、配置、追跡状態を明示指示なしに変更しない。
- `train_titanic.csv` のみに `Survived` がある。testの正解ラベルを生成・補完しない。
- 現在の依存関係は `ipykernel` だけである。分析タスクが確定するまでpandasやscikit-learnを追加しない。
- `main.py` は雛形であり、完成したTitanic実装として扱わない。

## 完了条件

準備状態と未確定事項を明記し、CSVが未変更・未追跡のままであること、環境定義に依頼外変更がないことを確認する。
