# AGENTS.md - Lesson 6

## 適用範囲

このファイルは `lesson_6/` に適用し、共通規則は `../AGENTS.md` を継承する。

## 固有ルール

- 入力zipは現在 `../lesson_7/MNICT_train.csv.zip` にある。明示指示なしにlesson間で移動・複製せず、実行時は使用した実パスを報告する。
- `assignment.cord.第6回.ipynb` の `/Users/t-shuichi/...` は既知のfallbackであり、新しい端末固有パスへ置換しない。
- `assignment.cord.第6回.ipynb` は入力データと同じ場所へ複数の解析CSVを書き出す。同期領域の原本でそのまま実行せず、出力先を確認して `/tmp` の作業コピーを使う。
- `MNICT_train_label.csv` は現在存在しない。Notebook前半ではラベル比較をスキップするが、後半のデンドログラム評価では必須となるため、全セルが完走できると記載しない。
- ID列とpixel列を確認し、IDをPCAやクラスタリングの特徴量へ混入させない。0分散列の扱いも確認する。
- PCAの寄与率とローディング、UMAP/t-SNEの軸の意味を混同しない。クラスタ番号を正解ラベルとして解釈しない。
- 重い処理は固定seedのsubsetで先に検証し、全件実行と同じ結果であるかのように記載しない。

## 完了条件

使用データ、出力先、行数、pixel列数、seed、PCA寄与率、クラスタ数、評価指標と、ラベル有無および完全実行の有無を報告する。
