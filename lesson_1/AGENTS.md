# AGENTS.md - Lesson 1

## 適用範囲

このファイルは `lesson_1/` に適用し、共通規則は `../AGENTS.md` を継承する。

## 固有ルール

- この回の目的はPython 3.11とuv環境、サーバ接続、Git/GitHubの基礎導入であり、`main.py` を授業実装へ拡張しない。
- 演習NotebookやCSVは存在しない。依頼なしに新しい課題や依存関係を補わない。
- 2つの第1回PPTXは別ファイルとして維持し、内容比較なしに統合、改名、上書きしない。
- Officeの一時ファイル `~$...pptx` は成果物ではないため、編集・追跡しない。

## 完了条件

文書またはPPTXの対象版を明示し、環境確認時は `uv lock --check` と `uv sync --frozen` の結果を報告する。
